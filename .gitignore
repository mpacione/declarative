# Python
__pycache__/
*.pyc
*.pyo
.coverage
.pytest_cache/
.venv/

# SQLite databases (generated, not source)
*.declarative.db
backups/
.db-backups/

# Archived experiments (preserved locally, not tracked)
experiments/archive/

# Old / one-off render batch variants (if they reappear)
render_batch_v*/

# Render-test debug scratch (experimental, not load-bearing)
render_test/screen_*.js
render_test/simple.js
render_test/throw_test.js

# macOS
.DS_Store

# Temp scripts from debugging sessions
extraction_runner.py
extract_with_real_data.py
run_extraction_with_mcp.py

# Environment / secrets
.env
.env.*

# Misc
=2.28
.code-graph/

# Node (bridge runner deps live here; ws is installed via `npm install`)
node_modules/
