# archive/

Non-doc-non-code artifacts preserved out of the primary tree:
- `db-snapshots/` — pre-migration .bak.db files
- `scratch/` — old tmp/, .coverage, session scratch

Kept tracked (not gitignored) so a future Claude session can find
historical state if a regression points back here.
