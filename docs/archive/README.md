# docs/archive/

Historical planning / continuation / one-shot docs preserved for
provenance. Nothing here is load-bearing for current code; nothing
in `docs/` or `dd/` imports or cites these by path.

If you're a new session: read `ENTRYPOINT.md` at the repo root
first. This folder is NOT the starting point.

## What's in here
- Superseded plans (plan-v0.3.md, plan-nested-compref-option-2.md, ...)
- Archived continuation docs
- t5-* research cluster (subfolder: research/)
- Early-stage snapshot docs (compression-profile.md, tier-progress.md, ...)
