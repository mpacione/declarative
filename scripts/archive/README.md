# scripts/archive/

One-off demos + bakeoff scripts from shipped M7.x milestones.
Preserved for reference; NOT maintained. Prefer `dd design` (Stage 3)
for the current authoring loop.

If you need a demo for a shipped feature, try:
- `dd design --brief "..."` — full session loop (Stage 3)
- pytest `tests/test_propose_edits_capstone.py` — Stage 1 contract
- pytest `tests/test_stage2_acceptance.py` — Stage 2 primitives
